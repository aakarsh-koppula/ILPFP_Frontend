import { SearchCategoriesPipe } from './search-categories.pipe';

describe('SearchCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
